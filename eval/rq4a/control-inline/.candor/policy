# Architecture-as-code. Enforced by candor (AS-EFF-006).
# The pricing domain is the pure core: it must perform NO effects of any kind.
# All I/O belongs in the application/infrastructure layer.
pure pricing
